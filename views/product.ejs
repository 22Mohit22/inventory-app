<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <div class="app">
        <%- include('./header') %>
        <div class="content">

            <div class="product_big_card">
                <%= product.item %>
                <%= product.price %>
                <%= product.stock_qty %>
                <%= product.description %>
                <a href="/categories/<%= product.category %>"><%= product.category %></a>
                <%= product.supplier %>
            </div>
        </div>
    </div>
</body>
</html>